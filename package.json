{
  "name": "tree-sitter-nova",
  "version": "0.0.2",
  "main": "index.js",
  "scripts": {
    "start": "tree-sitter generate && tree-sitter parse test.nova",
    "build": "tree-sitter generate && node-gyp rebuild",
    "test": "tree-sitter test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Keshyu/tree-sitter-nova.git"
  },
  "dependencies": {
    "nan": "^2.14.2"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.17.3"
  }
}
